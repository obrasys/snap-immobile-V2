import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.neonarmadilloroll',
  appName: 'neon-armadillo-roll',
  webDir: 'dist'
};

export default config;
